// main.rs - Default binary (redirects to CLI)
fn main() {
    eprintln!("Note: The CLI binary has been moved. Use:");
    eprintln!("  cargo run --bin cli      # for command-line interface");
    eprintln!("  cargo run --bin server   # for web server demo");
}
